BACKEND TESTING (Node + Express + MongoDB + MySQL)

🔹 Tools (Industry Standard)

Jest → Test runner
Supertest → API testing
MongoDB Memory Server → mock MongoDB
MySQL test DB (separate schema)

What You Must Test (FAANG mindset)

Layer	What to Test
Route	Status codes, responses
DB	Data correctness
Edge Cases	invalid input
Failure	DB down, errors
Combined API	consistency across DBs

FAANG-Level Concept → Data Consistency Problem

❗ What if:

Mongo succeeds ❌
MySQL fails ❌

👉 You must mention:

Rollback strategy
Retry mechanism
Event-driven approach
💡 Example Answer:

“In combined APIs, I ensure eventual consistency using retries or queue-based systems like Kafka.”



